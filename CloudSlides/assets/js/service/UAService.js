// Generated by CoffeeScript 1.7.1
(function() {
  angular.module('UAService', []).factory('UAService', function() {
    var getAndroidVersion, isChrome, isMiuiBrowser, isQqBrowser, isTargetBrowser, isUcBrowser, obj, userAgent;
    userAgent = navigator.userAgent.toLowerCase();
    getAndroidVersion = function() {
      var androidVersion, index;
      index = userAgent.indexOf("android");
      if (index >= 0) {
        androidVersion = parseFloat(userAgent.slice(index + 8));
        console.log(androidVersion);
        return androidVersion;
      } else {
        return -1;
      }
    };
    isTargetBrowser = function(browserKeyword) {
      if (userAgent.indexOf(browserKeyword) >= 0) {
        return true;
      } else {
        return false;
      }
    };
    isUcBrowser = function() {
      return isTargetBrowser('ucweb');
    };
    isQqBrowser = function() {
      return isTargetBrowser('mqqbrowser');
    };
    isChrome = function() {
      return isTargetBrowser('chrome');
    };
    isMiuiBrowser = function() {
      return isTargetBrowser('miuibrowser');
    };
    obj = {};
    obj.isDownGraded = function() {
      if (isUcBrowser() || isQqBrowser() || isChrome()) {
        return false;
      } else if (isMiuiBrowser()) {
        return true;
      }
      return true;
    };
    obj.androidVersion = getAndroidVersion();
    obj.isAndroid = getAndroidVersion() > 0 ? true : false;
    return obj;
  });

}).call(this);

//# sourceMappingURL=UAService.map
